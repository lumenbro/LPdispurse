{
  "name": "lmnr-rewards-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "tsc src/cadence.ts src/ledger.ts --outDir .test-build --module esnext --target es2022 --moduleResolution bundler --skipLibCheck --types @cloudflare/workers-types && node test/cadence.test.mjs"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
