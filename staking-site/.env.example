# === Contract ===
NEXT_PUBLIC_CONTRACT_ID=C...your_contract_id
NEXT_PUBLIC_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"
NEXT_PUBLIC_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org

# === Indexer (server-side only) ===
ADMIN_SECRET_KEY=S...your_admin_secret_key
CRON_SECRET=your_vercel_cron_secret

# === Pool configuration (JSON array of pool hashes) ===
# Each entry: { "index": 0, "poolId": "hex_hash_of_stellar_amm_pool" }
POOL_CONFIG='[{"index":0,"poolId":"0101010101010101010101010101010101010101010101010101010101010101"}]'

# === Vercel Blob (auto-configured on Vercel, needed for local dev) ===
BLOB_READ_WRITE_TOKEN=vercel_blob_token
